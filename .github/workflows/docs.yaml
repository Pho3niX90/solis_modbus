name: Generate Docs

on:
  push:
  workflow_dispatch:

jobs:
  validate:
    runs-on: "ubuntu-latest"
    steps:
      - uses: readthedocs/actions/preview@v1
        with:
          project-slug: 'solis_modbus'